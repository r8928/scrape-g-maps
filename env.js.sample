const API_KEY = "";

const BASE_URL = "";

const SEARCH_PREFIX = " ";

const SEARCHES = [];

module.exports = {
  SEARCH_PREFIX,
  SEARCHES,
  API_KEY,
  BASE_URL: BASE_URL.replace("%key%", API_KEY),
};
