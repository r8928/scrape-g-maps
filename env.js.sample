const API_KEY = "";

const BASE_URL =
  "https://maps.googleapis.com/maps/api/place/textsearch/json?key=%key%";

const SEARCH_PREFIX = " ";

const SEARCHES = [];

module.exports = {
  SEARCH_PREFIX,
  SEARCHES,
  API_KEY,
  BASE_URL: BASE_URL.replace("%key%", API_KEY),
};
